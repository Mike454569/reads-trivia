# Reads v3.5 — Production Autopilot

Orchestrates content generation, health checks and explicit publication gates. Autopilot can HOLD content; it never treats a failed QA/freshness/release gate as publishable.

# Quiz Engine Pilot -- Audit Report

Generated by `tools/export_quiz_engine_pilot.py`, seed `reads-quiz-engine-pilot-v1`.
Not wired into the running app. Output: `data/quiz-engine-pilot.js`.

## Production-safety gate (checked once, before any candidate generation)

- **domain_id**: NFL_DRAFT
- **competition_id**: NFL
- **dataset_name**: draft/player seed
- **coverage_start**: 1980
- **coverage_end**: 2024
- **completeness**: HISTORICAL_COMPLETE_FOR_SOURCE_RANGE
- **production_safe**: True
- **source_id**: NFLVERSE_DATA
- **source_name**: nflverse-data
- **approved_for_import**: True

## Engine feasibility check

- predicate: `DRAFTED_BY`, mechanic: `guess`
- `game_factory.feasibility()` status: **SUPPORTED**
- estimated candidates (Engine's own estimate): 12253
- reason: Backed by NFL draft data.
- source table: relationships

## Candidate funnel

- Candidates considered (raw output of `game_factory.generate_candidates()`, limit=500): **500**
- Rejected: **262**
- Accepted (passed every check): **238**
- Exported to `quiz-engine-pilot.js` (capped at pilot target): **50**
- Accepted but not exported (beyond the 50-question pilot cap): 188

### Rejection reasons

| Reason | Count |
|---|---|
| TEAM_UNRESOLVED | 251 |
| DUPLICATE_PLAYER | 10 |
| DUPLICATE_QUESTION | 1 |

Note: the ~52% rejection rate is dominated by `TEAM_UNRESOLVED` -- the Engine's own `team_aliases` team-code resolver only has season-aware coverage from 2002 onward, so pre-2002 `draft_facts` rows (and a handful of non-standard historical codes like `STL`/`OAK`/`LARM` outside their resolvable window) are rejected rather than guessed at. This is a byproduct of the verification step, not a year cutoff applied by this script.

## Exported set breakdown

### Counts by category

- NFL Draft History: 50

### Counts by difficulty

- Hard: 33
- Medium: 17

### Season range covered

- min season: 2002
- max season: 2024
- distinct franchises represented: 29 / 32

### Source / domain breakdown

- domain: `NFL_DRAFT` (production_safe=True)
- source: `NFLVERSE_DATA` (nflverse-data, approved_for_import=True)
- every exported row's underlying `draft_facts` record has verification_status=`SOURCE_BACKED`, source_id=`NFLVERSE_DATA` (checked per-row, not assumed)
- correct-answer and all distractor team names resolved via Engine's `team_aliases` table, season-matched to the player's actual draft year

### Duplicate check

- duplicate question text: 0
- duplicate player used twice: 0
- duplicate id values: 0

### QUIZ_DATA contract validation

**PASSED** -- all 50 exported objects have exactly the keys `id, category, difficulty, question, options, correctIndex, notes`; `difficulty` in {Easy, Medium, Hard}; `options` has exactly 4 unique strings; `correctIndex` is a valid 0-3 index pointing at the verified correct answer; `notes` is `""` for every row (the Engine's DRAFTED_BY/guess payload has no explanation field, so none was invented).

## Category mapping table used

| Engine predicate | Reads Quiz category |
|---|---|
| `DRAFTED_BY` | NFL Draft History |

## Difficulty mapping used

| Engine band | Quiz difficulty |
|---|---|
| EASY | Easy |
| MEDIUM | Medium |
| HARD | Hard |
| EXPERT | Hard |


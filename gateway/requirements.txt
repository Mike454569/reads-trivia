# Reads Engine Gateway -- dependencies (Director v0.6, Part B).
# Minimal on purpose: one web framework, its ASGI server, and its own
# validation library (pydantic ships with FastAPI but pinning it explicitly
# documents that this Gateway depends on it directly for gateway/models.py).
# httpx is test-only (gateway/tests/, drives the app in-process via
# FastAPI's TestClient) -- not imported by any runtime route/service code.
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.4
httpx==0.28.1
# beautifulsoup4: real production gap found live -- tools/data_refresh/
# cfb_all_america_import.py (Engine-gap-audit operation) imports bs4 to
# parse Wikipedia's All-America selection tables (stdlib html.parser
# backend, no lxml needed), but it was never added here. Since
# gateway/services/admin_refresh.py's _runners() imports every
# tools.data_refresh dataset module in ONE combined statement, this
# ImportError broke EVERY dataset refresh in production, not just
# cfb_all_america -- confirmed live via a real 503 SERVICE_UNAVAILABLE
# ("No module named 'bs4'") triggering an unrelated dataset (cfb_betting_lines).
beautifulsoup4==4.15.0
# tzdata: Dynamic Weekly Pick'em pass -- tools/data_refresh/_pickem_status.py
# and tools/director_v04/weekly_pickem.py use zoneinfo.ZoneInfo("America/
# New_York") to combine NFL's separate game_date/game_time columns into a
# real UTC kickoff. python:3.11-slim-bookworm (gateway/Dockerfile) is not
# guaranteed to ship IANA tzdata -- this pure-Python fallback package makes
# zoneinfo work regardless of the base image's OS tzdata, the standard-
# recommended way to make it deployment-independent.
tzdata==2025.2
